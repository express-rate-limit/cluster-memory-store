// /source/types.ts
// The type definitions for this package.

/**
 * The configuration options for the store.
 */
export type Options = {
	/**
	 * Optional unique identifier to avoid double-counting when using multiple instances
	 */
	prefix: string
}
